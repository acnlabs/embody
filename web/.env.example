# Same Auth0 tenant as ComicLaw / AgentPlanet; Embody has its own SPA client
# (callback /auth/callback).
NEXT_PUBLIC_AUTH0_DOMAIN=dev-ypufda63738rkary.us.auth0.com
NEXT_PUBLIC_AUTH0_CLIENT_ID=u5v4HwPl2mSHuEQKKppD7XbmEIi54p7q
NEXT_PUBLIC_AUTH0_AUDIENCE=https://api.agentplanet.org

# Agent push: verify ACN_API_KEY via /agents/me. The real owner agents live on
# the .dev network (AgentPlanet proxies it); acn.acnlabs.cn is a separate
# China-side deployment with its own key space.
ACN_BASE_URL=https://api.acnlabs.dev

# Owner identity only (which agents this human owns). Not the body ledger.
AGENTPLANET_API_URL=https://api.agentplanet.org

# Optional. Interfaze wallet link when chat is out of Credits.
# NEXT_PUBLIC_AGENTPLANET_WALLET_URL=https://agentplanet.org/wallet

# Mint iframe host tokens (Interfaze Body window). Required in production.
# openssl rand -hex 32
HOST_TOKEN_SECRET=

# Body ledger. On Vercel: attach a KV store and these are injected.
# Locally unset = file ledger at web/.data/ledger.json.
KV_REST_API_URL=
KV_REST_API_TOKEN=
